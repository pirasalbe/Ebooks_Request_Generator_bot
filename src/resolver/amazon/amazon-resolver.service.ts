import { Resolver } from './../resolver';

export class AmazonResolverService implements Resolver {
  resolve(url: string): Promise<string> {
    // TODO
    throw new Error('Method not implemented.');
  }
}
